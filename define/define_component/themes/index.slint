export global THEMES {
    out property <brush> theme-color : #5b64a8;
    out property <brush> theme-hover : #3542a3;
    out property <length> font-size : 16px;
    out property <{
        top-bottom: length,
        left-right:length
    }> padding : {
        top-bottom: 8px,
        left-right:16px
    };
}